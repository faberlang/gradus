+++
locale = "en"
+++

# FAP-1 red 1 — no `@ nucleum` module in `gradus:` today.
#
# The import names the module the pilot will author (gradus/src/kernel.fab,
# module `gradus:kernel`). It fails today with PKG001 unknown_library_module:
# no gradus library module carries a `@ kernel` (`@ nucleum`) entry yet.

import from "gradus:kernel" kernel

fn main() → vacuum {
    print 1
}
