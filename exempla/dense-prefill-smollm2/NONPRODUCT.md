# Non-product — GI2 golden dumps

The `gi2-*.txt` files in this directory are tracked development golden
dumps from GI2 bring-up. They are not part of the public `gradus:*`
product surface (`cista.toml` ships `src/` only).
