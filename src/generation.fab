# Generation package map.
#
# Public import: import from "gradus:generation" generation
# Prefer leaves:
#   gradus:generation/config    GenerationConfig, GenerationError, acceleration
#   gradus:generation/decoder   cursor, stop policy, decoder generate
#   gradus:generation/dense     DenseEngine and dense generate
#
# This file holds no genera. Import the leaf that owns the type.

import from "gradus:generation/config" config
import from "gradus:generation/decoder" decoder
import from "gradus:generation/dense" dense_gen
