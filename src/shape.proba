+++
locale = "en"
+++
# Co-located package test for shape.fab (PML1-U3 shape system).
# Not importable; discovered only by the faber test path.
#
# Proves, at compile level: the staged-carrier shape rules — introspection
# (valet / gradus / quantitas), broadcast (right-aligned pairwise, singleton
# 1 dims), reshape (element-count preservation, single -1 inference), expand
# (left-pad singleton dims to a target rank), and the general bounds checks
# (negative dims, element-count ceiling, rank ceiling) with distinct
# FormaError identity. The per-dimension 65536 (GI1 pinned-row) ceiling is a
# support-row/capsule admission fact, NOT general arithmetic: general shape
# math admits vocab-scale rows (128k–152k) within the element ceiling
# (CTO-2 correction, PML1 closeout).
# adfirma assertions run under the cargo-backed `faber test` harness
# (auditor-owned); `faber check` proves the whole surface type-checks.
import from "gradus:shape" shape

# -- introspection ---------------------------------------------------------

test "valet accepts non-negative shapes" {
    do {
        assert shape.valid([2, 3])
        assert shape.valid([])
        assert shape.valid([0, 0])
    }
    catch err {
        assert false
    }
}

test "valet rejects a negative dimension" {
    do {
        assert not shape.valid([1, -1])
    }
    catch err {
        assert false
    }
}

test "gradus reports the rank" {
    do {
        assert shape.rank([2, 3]) ≡ 2
        assert shape.rank([]) ≡ 0
        assert shape.rank([2, 3, 4]) ≡ 3
    }
    catch err {
        assert false
    }
}

test "quantitas is the element product" {
    do {
        assert shape.numel([2, 3]) ≡ 6
        assert shape.numel([]) ≡ 1
        assert shape.numel([2, 0, 3]) ≡ 0
    }
    catch err {
        assert false
    }
}

# -- bounds and ceilings (GI1 precedent) ------------------------------------

test "quantitas rejects a negative dimension" {
    do {
        shape.numel([-1])
        assert false
    }
    catch err {
        assert shape.message(err) ≡ "negative dimension"
    }
}

test "quantitas admits a vocab-scale dimension (no general per-dim cap)" {
    do {
        assert shape.numel([70000]) ≡ 70000
        assert shape.numel([152064]) ≡ 152064
        assert shape.numel([128000]) ≡ 128000
    }
    catch err {
        assert false
    }
}

test "quantitas rejects an element count above the ceiling" {
    do {
        shape.numel([1000000000, 2])
        assert false
    }
    catch err {
        assert shape.message(err) ≡ "element count above ceiling"
    }
}

# -- broadcast --------------------------------------------------------------

test "broadcast right-aligns and pads with singletons" {
    do {
        assert shape.broadcast([2, 3], [3]) ≡ [2, 3]
        assert shape.broadcast([2, 3], []) ≡ [2, 3]
        const list<int> scalar ← shape.broadcast([], [])
        assert shape.numel(scalar) ≡ 1
    }
    catch err {
        assert false
    }
}

test "broadcast unifies singleton dimensions" {
    do {
        assert shape.broadcast([1, 4], [2, 1]) ≡ [2, 4]
        assert shape.broadcast([2, 3], [1, 3]) ≡ [2, 3]
        assert shape.broadcast([3, 1], [2, 3, 1]) ≡ [2, 3, 1]
    }
    catch err {
        assert false
    }
}

test "broadcast rejects incompatible dimensions" {
    do {
        shape.broadcast([2, 3], [4, 3])
        assert false
    }
    catch err {
        assert shape.message(err) ≡ "shapes not broadcastable"
    }
}

test "broadcast rejects a negative input dimension" {
    do {
        shape.broadcast([2, -1], [3])
        assert false
    }
    catch err {
        assert shape.message(err) ≡ "negative dimension"
    }
}

# -- reshape ----------------------------------------------------------------

test "reshape preserves the element count" {
    do {
        assert shape.reshape([2, 3], [6]) ≡ [6]
        assert shape.reshape([2, 3], [3, 2]) ≡ [3, 2]
        assert shape.reshape([2, 3], [1, 2, 3]) ≡ [1, 2, 3]
    }
    catch err {
        assert false
    }
}

test "reshape infers a single -1 dimension" {
    do {
        assert shape.reshape([2, 3], [-1]) ≡ [6]
        assert shape.reshape([2, 3], [2, -1]) ≡ [2, 3]
        assert shape.reshape([2, 3], [-1, 1]) ≡ [6, 1]
    }
    catch err {
        assert false
    }
}

test "reshape rejects an element-count mismatch" {
    do {
        shape.reshape([2, 3], [4])
        assert false
    }
    catch err {
        assert shape.message(err) ≡ "element count mismatch"
    }
}

test "reshape rejects more than one inferred dimension" {
    do {
        shape.reshape([2, 3], [-1, -1])
        assert false
    }
    catch err {
        assert shape.message(err) ≡ "at most one inferred dimension"
    }
}

test "reshape rejects a non-integral inferred dimension" {
    do {
        shape.reshape([2, 3], [5, -1])
        assert false
    }
    catch err {
        assert shape.message(err) ≡ "inferred dimension not integral"
    }
}

test "reshape rejects a negative dimension other than -1" {
    do {
        shape.reshape([2, 3], [-2])
        assert false
    }
    catch err {
        assert shape.message(err) ≡ "negative dimension"
    }
}

test "reshape admits an inferred vocab-scale dimension (no per-dim cap)" {
    do {
        assert shape.reshape([152064, 2], [-1]) ≡ [304128]
        assert shape.reshape([65536, 2], [-1]) ≡ [131072]
    }
    catch err {
        assert false
    }
}

# -- expand ----------------------------------------------------------------

test "expand pads leading singleton dimensions" {
    do {
        assert shape.expand([2, 3], 2) ≡ [2, 3]
        assert shape.expand([2, 3], 4) ≡ [1, 1, 2, 3]
        assert shape.expand([], 3) ≡ [1, 1, 1]
    }
    catch err {
        assert false
    }
}

test "expand rejects a target rank below the current rank" {
    do {
        shape.expand([2, 3], 1)
        assert false
    }
    catch err {
        assert shape.message(err) ≡ "target rank below current rank"
    }
}

test "expand rejects a target rank above the rank ceiling" {
    do {
        shape.expand([2], 65)
        assert false
    }
    catch err {
        assert shape.message(err) ≡ "target rank above ceiling"
    }
}

test "expand rejects a negative input dimension" {
    do {
        shape.expand([1, -1], 2)
        assert false
    }
    catch err {
        assert shape.message(err) ≡ "negative dimension"
    }
}
