{
  "schema": "pgc-r1-fma-staging-census-v1",
  "scope": "fixed-1000 prefill embedding gather (T=36, V=49152, D=960), per prefill invocation",
  "two_class_oracle": "byte identity (class B2-analog): a row copy has no rounding surface — `out[t] = table[ids[t]]` moves f32 bits untouched, so byte-identical outputs are the correct class; no tolerance is invoked or widened",
  "before": {
    "operation": "dense one-hot selector matmul [36,49152] · [49152,960] (decode member [1,49152]·[49152,960]), 40-row-padded dispatch [40,49152,960]",
    "dispatched_gemm_class_fmas": 1887436800,
    "useful_row_values": 34560,
    "staged_selector_bytes": 7077888,
    "source": "findings 06a62904 + 051731ac (gradus/src/decode.fab:512-517 list-append staging → selector matmul; gradus/src/kernel.fab:632 selector body, superseded)"
  },
  "after": {
    "operation": "indexed row-copy gather out[t,d] = table[ids[t],d], one thread per output element, workgroup (1,1,1) over 34560 threads (decode: 960)",
    "dispatched_gemm_class_fmas": 0,
    "useful_row_copy_element_copies": 34560,
    "staged_ids_bytes": 144,
    "staging_delta_bytes": -7077744,
    "proof": "radix gea3_pipeline_pgc_r1_test (emitted-body census) + hosts gea3_decode_pgc_r1 (device-boundary census)"
  },
  "wall": "none claimed (condition-B rider: FMA/staged-byte census is the primary evidence; wall observations belong to PGC-R3's family-keyed capture)"
}
