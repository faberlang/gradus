module=src/calibration.fab
loop_sites=23
closure_intrinsic_sites=3
warn027_complexity_rows=2
warn027_kernel_rows=2
conversions=tot energy fold -> .reduce; per-K residual fold -> slice+reduce; _scale -> .map; _digest_ok slice-per-index -> for-from + between membership (deleted _hex helper)
keepers=_dot/_sub_scale (two-list index walks), _zeros (no fill construct), _put_f32 (in-place write emulation), _finite_vec (early-exit predicate), bake recommendation scan (first-K latch), _residual2 (projection square), _cosine (triple accumulator), bake/_validate_corpus scatter loops (ordered fail-closed)
air_tier_counts=not-observable (exact AIR command emitted zero rows for imported module)
