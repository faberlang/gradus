command=complexity-budget-12
count=2
calibration.fab:321 fn bake — in-code reason: one staged §17 measurement pipeline (slot scatter, curve fold, recommendation scan, score assembly); fail-closed order is the contract
calibration.fab:467 fn _validate_corpus — in-code reason: fail-closed ordered require validation plus routing-mass scatter; no construct covers ordered failable validation
command=kernel-complexity-budget-2
count=2
calibration.fab:321 fn bake — same survivor, same in-code reason
calibration.fab:467 fn _validate_corpus — same survivor, same in-code reason
note: calibration.fab has no @ kernel-marked functions; the kernel budget applies file-wide. Counts identical before/after (2).
