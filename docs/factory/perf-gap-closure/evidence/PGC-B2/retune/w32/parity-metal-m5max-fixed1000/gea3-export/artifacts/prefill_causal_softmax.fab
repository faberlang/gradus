@ kernel
@ public
fn prefill_causal_softmax(tf32[36,36] scores, tf32[36,36] causal_mask, mut tf32[36,36] output) → void {
    const f32 _stability_max ← max from scores at [i, j] coalesce 0.0
    output ← (scores + causal_mask).softmax()
}