@ kernel
@ public
fn decode_masked_softmax(tf32[1,76] scores, tf32[1,76] length_mask, mut tf32[1,76] output) → void {
    const f32 _stability_max ← max from scores at [i, j] coalesce 0.0
    output ← (scores + length_mask).softmax()
}