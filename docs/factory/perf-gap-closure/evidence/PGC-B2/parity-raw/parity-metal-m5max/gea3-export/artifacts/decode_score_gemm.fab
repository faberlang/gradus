@ kernel
@ public
fn decode_score_gemm(tf32[1,64] query, tf32[64,76] key_transposed, tf32[1,76] attention_scale, mut tf32[1,76] output) → void {
    output ← (query · key_transposed) ⊙ attention_scale
}