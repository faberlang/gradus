command=complexity-budget-12
count=6
block_verify.fab:189 fn rows_equal — in-code reason: pairwise staged-tensor equality, early return on first mismatch, per-index get-coalesce gathers over four parallel lists; no zip/equality construct covers staged NumericBlock carriers
block_verify.fab:230 fn construct_rows — in-code reason: failable per-row/per-layer validation in first-failure order; a map/reduce cannot carry the typed failure boundary
block_verify.fab:273 fn _admit_model — in-code reason: failable per-layer/per-token require chain in first-failure order; no covering construct carries the typed boundary
block_verify.fab:427 fn recompute — in-code reason: stateful scalar replay (step/layers updated per token through the dense seam); no covering construct for a fold over stateful failable steps
block_verify.fab:562 fn oracle_equal — in-code reason: earliest-divergence comparison with early-return reports across positions/logits/history/staged tensors (index-space flat scan); no covering construct for a first-divergence search
block_verify.fab:675 fn construct_verification — in-code reason: bounded acceptance/identity admission is a require chain, not loop debt; complexity is the admission contract itself
command=kernel-complexity-budget-2
count=6
(same six rows and reasons as complexity-budget-12)
note: imported-module WARN027 rows outside PGC-A4 write scope are unchanged before/after (see warn027-before.rows note).
