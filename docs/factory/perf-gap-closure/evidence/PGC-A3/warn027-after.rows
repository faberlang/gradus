command=complexity-budget-12
count=2
math.fab:495 fn matmul — in-code reason: index-space dot products over NumericBlock carriers; typed tensor-intrinsic operands not available on the carrier surface
math.fab:598 fn concatenate — in-code reason: failable per-part validation order plus interval-membership part selection; no covering construct
command=kernel-complexity-budget-2
count=2
math.fab:495 fn matmul — same reason
math.fab:598 fn concatenate — same reason
note: complexity-before.txt / complexity-after.txt each additionally carry 2 WARN027 rows in imported src/shape.fab (lines 158/203); shape.fab is outside PGC-A3 write scope and its rows are unchanged before/after
